1. Claude calls my engineered code to human which uses many types of variables to or a simple skill or CLAUDE.md to generate the new graph. Which then can be used to analyze how a code works.

2. the node text should be different. it should show code in the human text itself, maybe place holder and pseudocode to make it simpler to understand.

3. the web's function is to be an unmodified visualizer. to not be touched. that way i can focus only on the compilers understanding of:
- how deep to create nodes and layers of abstraction. this shouldn't be an issue unless too many nodes.
- what's the structure of each node. it shouldn't be an issue with a good compiler.
- (i'm not sure yet) but the internal node mapping of the text to their corresponding code. it shouldn't be complex if i don't allow editing the text through the web. and if the AI or the human modifies the text, the compiler is capable of restructuring properly.
- when the compiler can throw error. example: i copy and paste the parent's text into the child text. what happens there? does the compiler throw or does it try to create code about it?
- what rules does the compiler create or form based on the language used by the human?
