@id: 3d7f
draw the sentences as a layered graph of range containment on the left, covering edges only and never transitive ones, the root on top: a sentence hangs below every parent whose range most tightly contains one of its own, several parents allowed, which the folder tree cannot show
