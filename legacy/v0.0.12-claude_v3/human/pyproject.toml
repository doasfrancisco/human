[project]
name = "human"
version = "0.3.0"
description = "Owner-set causation maps declared at generation, gated by three computed invariants."
requires-python = ">=3.11"
dependencies = [
    "boto3>=1.35",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
