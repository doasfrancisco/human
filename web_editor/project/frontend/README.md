# v0.0.5 frontend

Next.js + CodeMirror editor for fran++ v0.0.5.

## Run

Start the FastAPI backend first on `http://localhost:8000`, then:

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.

Set a custom backend URL with:

```env
NEXT_PUBLIC_API_URL=http://localhost:8000
```
