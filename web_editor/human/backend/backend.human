load the nearest env file up the tree and set up fastapi with cors open to any localhost port
pydantic models typing every request and response payload
a multi project registry in projects.json with an active pointer and ways to create select rename wipe projects
a content addressed compile graph with sha256 snapshots of human context and python each carrying a language key stored under program names that can nest with slashes turned into double underscores in the graph filename where going back to an old snapshot restores its whole chain and deletes cascade down
a phrase memo that remembers segmentation and snaps phrases to whole word groups never splitting number runs
per line provenance labeling each context line added inherited updated or manual with the closest previous line by similarity and every split context line attributed to its phrase by the llm alone and stamped with a stable phrase id from the graph's phrase registry that answers hydrate back into the phrase's current wording with line attribution always llm and no read time backfill so old graphs show nothing until re adopted and entries without an id pass through unchanged where hand edits carry existing entries over by text keeping their phrase ids only genuinely new lines call the llm and take an id from their attributed phrase and a fully carried save makes zero llm calls
materialize the active files into the workspace with an explain report unlinking stale outputs of other extensions and mirror them into the repo human and project folders writing split units as their own files creating parent directories for every output so nested names land cleanly
a history tree of humans contexts and pythons with previews truncated at 72 chars and active flags
every llm stage calls claude on aws bedrock through boto3 converse at temperature 0 with system prompts from builder functions that stay word for word the same for python including one word triage of leaf vs split
attribute each split section header validated against an extension whitelist allowing forward slash nested relative paths and each split context line to the phrase that motivated it with llm json assignments retried once then failing with a 502 surfaced on the frontend and no keyword fallback where the section parsing line numbering and header regexes live in their own pure attribution module that main.py imports map every split unit's code lines back to the context line and phrase behind them under the same retry then 502 rule with single phrase programs skipping the llm and citing the whole human text stored on compile and adopt hydrated from the registry's current wording when answering so reword follows automatically with unit lines copying the phrase id of the context line behind them and relinked under the new context on hand edits with context lines remapped by text falling back to the section header for deleted lines and also map direct code lines back to verbatim phrases
adaptive compilation that reuses cached results unless forced updates the previous leaf incrementally and compiles each split section into its own child tagged with its target deriving the child's language from the target's own extension
filesystem endpoints with a recursive directory tree a native windows folder picker through powershell and raw file read write create delete
rest endpoints to save reword checkout delete bundle and compile everything with an optional language field that defaults to python and 400s on unknown values where saving over a split context keeps it split reword on a split program carries the exact old and new phrase pair from the editor and rewrites no stored provenance just the matching registry entries text and line plus the phrase memo mirror so every link follows the new wording with no llm and still no recompile and a save unit endpoint records edited unit code with its provenance recomputed by the llm retried once then a 502 plus a files endpoint that exposes the phrase registry and each entry's phrase id and seeds its answer from the repo human folder on disk when the graph is still empty writing nothing until the first save
run it with uvicorn via the fastapi cli with deps declared in pyproject
